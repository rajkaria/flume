{
  "name": "@flume/contracts",
  "version": "0.1.0",
  "description": "Solidity contracts on Arc — FlumeRegistry, EscrowVault, RevenueSplit",
  "type": "commonjs",
  "scripts": {
    "test": "echo 'hardhat test — fix ESM config before running'",
    "build": "echo 'hardhat compile — fix ESM config before running'",
    "typecheck": "echo 'contracts typecheck pending'",
    "lint": "echo 'lint placeholder'",
    "clean": "rm -rf artifacts cache typechain-types"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
